whiteboard/__pycache__/
**/__pycache__/
*.pyc
.pilot/
.stp-upgrade-cache.json
node_modules/
.env
.env.*
.DS_Store
*.log
.claude/settings.local.json

# Local-only — NEVER distributed to users via STP plugin
# The plugin's own repo dogfoods STP, so these contain local dev state,
# not plugin source. Users installing STP get fresh .stp/ and .claude/
# directories in their own projects.
.claude/commands/
.stp/
