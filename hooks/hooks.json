{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/post-edit-check.sh\"",
            "timeout": 30
          }
        ],
        "description": "Stack-aware type/compile check after editing source files"
      }
    ],
    "Stop": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/stop-verify.sh\"",
            "timeout": 60
          }
        ],
        "description": "Quality gate: stack-aware type check + tests + unchecked items. Exit 2 blocks stop. 3-attempt max prevents session bricking."
      }
    ],
    "PreCompact": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-compact-save.sh\"",
            "timeout": 10
          },
          {
            "type": "prompt",
            "prompt": "Context compaction is about to fire. Commit any uncommitted work now. State saved to .stp/state/state.json. After compaction, run /stp:continue to resume."
          }
        ],
        "description": "Save state to disk before compaction destroys conversation context"
      }
    ],
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/migrate-layout.sh\" && bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-restore.sh\"",
            "timeout": 15
          }
        ],
        "description": "Auto-migrate old layout + restore context from disk after /clear or compaction"
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/stp-statusline.js\""
  }
}
